export const enum ESocialType {
  TELEGRAM = 'telegram',
  MAIL = 'mail',
  PHONE = 'phone',
  GOOGLECHAT = 'googlechat',
}

export const enum EButtonType {
  PREMIUM = 'premium',
  SECONDARY = 'secondary',
  DISABLE = 'disable'
}
export const enum ERole {
  ADMIN = 'admin',
  USER = 'user',
}

export const enum EWorkStatus {
  FULL = 'full',
  HALF = 'half',
  EMPTY = 'empty',
}

export const enum EPosition {
  FE = 'Frontend developer',
  BE = 'Backend developer',
  DESIGNER = 'Designer',
  QA = 'QA',
}

